# Interview Assistant 技术方案（可行性概览）

> 本文档用于快速评估项目技术可行性：**用了哪些技术、方案是否行得通**。
> 仅作概览，不展开实现细节、不罗列具体数据；前端为重点。

---

## 1. 项目是什么

一个 **AI 驱动的模拟面试练习工具**：粘贴职位描述创建面试，AI 逐题提问，用户用**语音**作答（朗读题目 + 录音转写），结束后生成多维评分报告。附带题库、简历-JD 匹配预检、成长趋势分析，以及**可选的摄像头表情/行为信号分析**（本地运行、画面不上传）。

一句话判断：**技术方案成熟可行**——前端以主流 React 技术栈 + 现成浏览器 API（录音、WebSocket、本地机器学习）即可完成全部核心体验；AI 与语音能力全部通过现成云服务接入，无自研算法负担。

---

## 2. 技术总览

| 层面 | 用到的技术 |
|------|-----------|
| 前端 | React + TypeScript + Vite + React Router（单页应用，支持 PWA / 原生壳打包） |
| 后端 | Go + Gin 框架 |
| 数据存储 | MySQL（业务数据）+ Redis（直播临时状态） |
| 实时通信 | WebSocket（面试直播） |
| AI 能力 | DeepSeek 大模型（出题/追问/评分） |
| 语音能力 | 阿里云语音（ASR 录音转写 + TTS 朗读） |
| 图像识别 | 阿里云 OCR（图片导入、JD 图片转文字） |
| 文件存储 | 阿里云 OSS（简历/JD 文件，服务端代理） |
| 登录 | WPS 开放平台 OAuth |

---

## 3. 前端技术详解（重点）

### 3.1 工程架构

- **React + TypeScript**：组件化 UI，类型安全；按页面（pages）、通用组件（components）、工具（lib）、接口（api）、状态（auth）、WebSocket（ws）、行为分析（behavior）分层组织。
- **Vite**：开发/构建工具，提供热更新与生产构建，固定端口并暴露局域网，便于手机经局域网访问。
- **React Router**：前端路由 + 页面级导航，配合**路由守卫**（未登录自动跳转登录页）。
- **登录态管理**：Context 保存用户与凭证；后端地址、令牌、用户信息集中管理，登录/登出全局生效。
- **PWA**：通过 Service Worker 提供应用壳离线能力，支持"添加到主屏幕"作为独立应用运行。
- **Capacitor**：可把同一套前端打包成 Android 原生 App，多端覆盖（网页 / 局域网手机 / 原生壳）。
- **样式方案**：CSS 分层（设计令牌、按钮、移动端适配、各页面样式），配合响应式布局适配桌面与手机。
- **工程配套**：单元测试（Vitest）、代码检查（oxlint）、类型检查（tsc）、图标资源生成脚本。

### 3.2 页面与功能地图

| 页面 | 主要功能 |
|------|----------|
| 登录页 | WPS 授权入口：跳转授权页 → 回调 → 一次性凭证换应用令牌 |
| 欢迎/首页 | 产品介绍、入口引导 |
| 创建面试页 | 四卡片式"面试准备"：岗位信息（JD 文本/图片 OCR/文件）、个人简历（上传解析/简历库挑选）、题库选择、面试配置（类型/人格/难度/风格/摄像头开关） |
| 面试列表/详情页 | 历史面试列表、面试详情与状态 |
| 面试房间页（核心） | 语音问答直播：AI 提问朗读、按住说话作答、答题倒计时、暂停/恢复、跳过、手动结束、断线重连 |
| 报告页 | 四维评分与总分、优点/问题/改进建议、表达分析、可选的行为信号卡片 |
| 题库页 | 题目增删改查、标星、按维度/岗位筛选、批量删除、导入（文本/图片/拖拽）、专项练习 |
| 趋势页 | 历史成绩成长曲线、按岗位/类型/来源筛选 |

### 3.3 语音交互链路（核心体验）

- **录音**：浏览器录音 API（MediaRecorder + getUserMedia）按住说话采集；支持不同浏览器音频格式，并统一转换为 16kHz PCM 便于语音识别。
- **语音转写（ASR）**：把录音上传阿里云语音接口转成文字，作为答案自动提交给 AI；失败时有重试与降级提示。
- **语音朗读（TTS）**：AI 提问后用阿里云语音合成播放；播放器基于浏览器音频 API，用音频分析器（AudioContext + Analyser）采集音量电平做播放状态反馈；支持静音、重听。
- **断线自愈**：面试直播走 WebSocket，断线后自动按退避策略重连；断线期间的回答先在本地暂存，重连成功后自动补发，保证不丢答案。

### 3.4 实时面试直播

- **WebSocket 连接封装**：统一处理消息类型（开始、提问、追问、状态、结束）、解析与分发，供页面层复用。
- **面试房间交互**：AI 思考态提示、答题倒计时（暂停冻结、恢复顺延、归零自动结束）、暂停/恢复、跳过当前题、手动强制结束、摄像头画面预览。

### 3.5 摄像头行为信号分析（可选、本地优先）

- **人脸关键点检测**：TensorFlow.js + FaceMesh（MediaPipe 人脸网格模型）在浏览器本地逐帧检测面部关键点。
- **启发式信号提取**：由嘴部开合、眼睑开合、眉毛高度、头部俯仰角等几何特征推导——情绪标签（微笑/专注/惊讶/皱眉等）、点头事件、紧张度指标（综合眨眼频率、头部晃动、表情切换）。
- **聚合与上报**：面试期间本地聚合统计，结束时仅上报聚合数据（情绪分布、点头次数、紧张度走势）到后端存档。
- **隐私与降级**：画面全程不出浏览器、不上传任何帧；默认关闭、需用户授权；浏览器不支持或模型加载失败时静默降级，不影响面试流程。
- **辅助定位**：分析结果作为报告中的"辅助参考"卡片，不计入正式评分。

### 3.6 文件与数据解析

- **简历解析**：本地解析多种格式——纯文本（txt/md 等）、Word（mammoth）、PDF（pdfjs 按页提取文字），识别失败给出明确提示。
- **图片 OCR 导入**：题库与 JD 支持上传图片，经后端 OCR 识别为文字后自动填入或生成候选题目。
- **拖拽上传**：创建页与导入模态框支持拖拽文件，含上传进度与类型校验。
- **文件上传**：简历/JD 文件经后端代理上传到 OSS，前端不直接接触存储凭证。

### 3.7 统一 API 层

- **请求封装**：统一 fetch 封装——自动附带登录凭证、统一超时、FormData/JSON 自动区分、错误码与用户文案映射、401 自动清理登录态并跳登录页。
- **按域拆分**：面试、题库、语音、OCR、上传、简历、趋势、画像、预检、表达、行为信号等各有独立 API 模块，类型定义与后端对齐。
- **自适应后端地址**：跟随当前页面地址自动推导后端地址，桌面本机、手机局域网、原生 App 三种场景无需改配置。

### 3.8 通用组件与交互

- **通用组件**：弹窗（Dialog）、确认框（ConfirmModal）、顶部导航（AppNav）、用户管理弹窗、题库导入模态框、摄像头预览等，跨页面复用。
- **列表与详情辅助**：岗位标签推导、面试来源识别（列表页跳转/趋势页跳转）、条目与详情组装等纯函数库，配套单元测试。
- **视觉设计**：基于设计令牌的统一配色/字号/间距体系，遵循已有视觉设计语言（见 DESIGN.md），适配深色应用场景。

---

## 4. 后端技术简述（满足概览即可）

- **Go + Gin**：HTTP 接口 + WebSocket，模块化组织（登录、面试、题库、评分、语音、OCR、上传、趋势、画像、预检、行为信号等独立模块）。
- **MySQL**：用户、面试会话、题目、答题轮次、题库、简历库、行为信号等持久化。
- **Redis**：面试直播状态（断线恢复）、OAuth 一次性凭证。
- **JWT**：接口鉴权；WPS OAuth 为唯一登录入口。
- **AI 编排**：DeepSeek 负责出题、追问决策、评分、题目解析/分类、岗位标题等，全程 JSON 结构化输出；服务端有硬规则兜底，保证流程不卡死。
- **外部服务均有降级策略**：对应密钥未配置时相应功能报错提示，不影响其他功能。

---

## 5. 结论

- **可行性：高。** 前端以主流 React 技术栈 + 现成浏览器 API（录音、WebSocket、本地机器学习）即可完成全部核心体验。
- **AI 与语音**均由成熟云服务承担，无需自研模型。
- **唯一强依赖**：AI 大模型与语音服务的密钥——缺了无法完成完整面试，属业务配置问题而非技术风险。

---

*本文档为可行性概览，实现细节与配置项以代码与部署文档为准。*
