# 面知前端开发方案（Web 端）

## 总体技术架构

系统采用前后端分离、React 单页应用（SPA）。前端为 **React 19 + TypeScript + Vite**，面向纯 Web 端（桌面浏览器），构建产物为静态资源，可部署到任意静态托管（Nginx 等）。与后端通过 REST（HTTP JSON）+ WebSocket 通信：**前端只做渲染、交互与浏览器本地能力（录音、播放、摄像头本地人脸推理）**，业务状态、AI 能力与持久化全部由后端提供。

按功能域拆包（`auth` / `api` / `ws` / `pages` / `components` / `lib` / `behavior`），模块之间通过模块内函数、hooks 与 props 协作，页面层不越层触碰接口协议细节。

### 模块边界与数据归属

模块之间只通过各自的封装接口交互，页面层不直接操作请求封装以下的内容。

|||||
|---|---|---|---|
|**模块**<br>|**负责**<br>|**不负责**<br>|**数据**<br>|
|`auth`<br>|WPS OAuth 登录态与用户上下文；授权回调换取登录；401 路由级跳转；路由守卫<br>|业务接口调用、页面渲染<br>|`localStorage(auth_token / auth_user)` + AuthContext<br>|
|`api`<br>|全部后端接口的请求封装（REST、multipart 上传、错误映射、401 处理）<br>|页面渲染、业务决策、本地状态<br>|无（纯请求层）<br>|
|`ws`<br>|面试直播 WebSocket 连接与协议封装（收发、心跳无关的断连回调）<br>|业务决策、重连策略之外的逻辑<br>|连接句柄 + 回调<br>|
|`pages`<br>|页面编排与交互（含房间页的语音链路/倒计时/重连编排）<br>|接口协议细节、纯函数实现<br>|组件本地 state / ref<br>|
|`components`<br>|跨页面复用 UI（弹窗、导航、预览、确认框、导入）<br>|业务逻辑、网络请求<br>|props<br>|
|`lib`<br>|纯函数工具（录音、播放、简历文本解析、枚举文案、来源判定）<br>|网络与渲染<br>|无<br>|
|`behavior`<br>|本地摄像头人脸关键点检测与行为信号采集/提取/聚合/上报（必开能力）<br>|除上报外的网络服务<br>|本地聚合缓冲<br>|

### 基础设施

||||
|---|---|---|
|**组件**<br>|**用途**<br>|**使用者**<br>|
|React + TypeScript<br>|UI 组件化与类型安全（React 19）<br>|全部页面 / 组件<br>|
|Vite<br>|开发与构建（固定端口 5174、严格端口校验）<br>|工程<br>|
|React Router v7<br>|前端路由与路由守卫（`ProtectedRoute`）<br>|App / 页面<br>|
|Recharts<br>|成长趋势图表<br>|TrendsPage<br>|
|TensorFlow.js + MediaPipe FaceMesh<br>|浏览器本地人脸 478 点关键点检测（行为分析）<br>|`behavior`<br>|
|mammoth / pdfjs-dist<br>|Word / PDF 简历文本解析（浏览器本地）<br>|CreateInterview / QuestionImportModal / ResumePreview / UserModal<br>|
|浏览器 Media API<br>|getUserMedia / MediaRecorder / AudioContext / OfflineAudioContext（录音、播放、电平）<br>|房间页 / `lib` / `behavior`<br>|
|localStorage<br>|JWT 令牌与用户缓存<br>|`api/client` / `auth`<br>|
|Vitest / oxlint / tsc<br>|单元测试、代码检查、类型检查<br>|工程<br>|

### AI 相关能力

前端侧的 AI 能力分为「浏览器本地运行」与「经后端接入」两类。

|||||
|---|---|---|---|
|**能力**<br>|**用途**<br>|**实现方式**<br>|**备注**<br>|
|语音识别 ASR<br>|用户语音转文字（面试唯一作答方式）<br>|录音转 16 kHz PCM 后 multipart 上传 `/api/speech/asr`<br>|后端接阿里云语音；识别失败可重试<br>|
|语音合成 TTS<br>|题目 / 追问 / 收尾语朗读<br>|`POST /api/speech/tts` 取音频播放<br>|单次合成 300 字上限，超长按句分段顺序播放；可静音 / 重播<br>|
|图片 OCR<br>|JD / 面经图片转文字<br>|multipart 上传 `/api/ocr/recognize`<br>|后端接阿里云 OCR<br>|
|表情 / 行为分析<br>|面试中实时情绪 / 点头 / 紧张度<br>|TensorFlow.js + FaceMesh 浏览器本地推理，仅上报聚合统计<br>|产品必开能力，画面不出浏览器；仅作报告辅助参考不计分<br>|
|出题 / 追问 / 评分 / 报告 / 发邮件<br>|业务智能<br>|调用对应后端业务接口<br>|后端接 DeepSeek，前端不直连模型；画像 / 预检接口已封装但当前页面未调用（预留）<br>|

---

## 模块职责

### auth（登录态）

- `AuthContext`：维护 `user` 状态；`loginWithWPS(code)` 用 WPS 回调带回的一次性 `oauth_code` 换取应用令牌；`logout()` 清空凭证；`refreshUser()` 从后端刷新资料。
- `ProtectedRoute`：无 `token` 或无 `user` 时重定向 `/login`。
- **401 处理**：`api/client` 收到 401 时清空登录态并派发 `app:unauthorized` 事件，`AuthProvider` 监听后做**路由级跳转**（SPA，不整页刷新）。`skipAuthClear` 场景（WPS 云文档子授权失效）不清登录态、不跳登录页。

### api（请求封装）

- `client.ts`：`getApiBase()` 后端地址解析（显式 `VITE_API_BASE` 优先，否则跟随当前页面 hostname 的 `:18080`）；`getToken()/setToken()` 令牌读写；`fetchJSON()` 统一封装（自动带 `Authorization: Bearer`、20s 超时、FormData 不覆盖 Content-Type 保证 multipart 可解析、401 清理并跳登录）；`ApiError` 与 `toUserMessage()` 错误文案中文化映射。
- 语音 / OCR / 图片导入等 multipart 请求走原生 `fetch`，避免 JSON 头污染。
- 各域模块：`auth` / `interviews` / `questions` / `speech` / `ocr` / `uploads` / `resumes` / `analytics` / `profile` / `precheck` / `expression` / `behavior` / `wps`，类型定义与后端对齐。

### ws（WebSocket）

- `interviewSocket.ts`：按面试 ID 与令牌建立连接（`/ws/interviews/:id?token=<JWT>`，WS 源由 API 基址推导）；封装消息解析；提供 `sendAnswer(content, voiceDurationMs?)` / `sendSkipQuestion()` / `close()`；回调 `onMessage` / `onClose` 供页面编排。

### pages（页面）

| 页面 | 职责 |
|------|------|
| WelcomePage | 落地页；已登录自动跳创建页 |
| LoginPage | WPS 授权登录（整页跳授权、回调换 token）；登录唯一方式为 WPS OAuth |
| CreateInterviewPage | 面试准备：岗位 JD、个人简历、题库勾选、提交创建 |
| InterviewListPage | 历史面试列表（状态/得分、看报告/进房间/详情、删除） |
| InterviewDetailPage | 面试详情（追问树展示、开始/继续/看报告、存入题库） |
| InterviewRoomPage | 直播房间（核心，见核心业务流程） |
| ReportPage | 评分报告、表达分析、行为信号、报告发邮箱 |
| QuestionBankPage | 题库管理（维度筛选、搜索、编辑、导入、删除） |
| TrendsPage | 成长趋势图表（岗位/模式/来源筛选） |
| NotFoundPage | 404 |

### components（复用 UI）

`AppNav`（导航，`sidebar` 左侧深蓝侧边栏 / `topbar` 顶部横条两种变体，房间沉浸页用 topbar）、`Dialog`、`ConfirmModal`（删除/结束确认）、`UserModal`（用户信息：WPS 授权同步资料、改昵称、换头像）、`CameraPreview`（摄像头画面预览，复用行为分析流避免重复开摄像头）、`QuestionImportModal`（题库导入：拖拽文件/图片自动 OCR/粘贴文本）、`ResumePreviewModal`（简历预览）、`FollowUpTree`（追问树展示）。

### lib（纯函数工具）

- `voiceRecorder`：`getUserMedia` 采集，MediaRecorder 录 webm，经 OfflineAudioContext 转 16 kHz PCM16 后上传。
- `voicePlayer`：TTS 音频播放 + AudioContext 分析器取音量电平（分析器初始化失败降级为纯播放）。
- `resumeParse`：txt/md/pdf/docx 简历文本提取（`.doc` 明确拒绝并提示另存）。
- `labels`：枚举值 → 中文文案（模式/状态/人格/难度/风格/维度/来源），与后端 `llm` 常量保持一致。
- `detailSource` / `listEntries`：详情来源判定（列表/题库/趋势）与列表入口生成（completed→看报告/详情，in_progress→进房间/详情）。
- `followUpTree`：把平铺 turns 构建为「主问题 → 追问 → 回答」两级树（按 seq 排序，异常容错不丢内容）。
- `demoTurns`：演示数据。

### behavior（本地行为分析）

- `cameraFeed`：`getUserMedia` 打开摄像头（本地流，可释放）。
- `FaceLandmarkDetector`：封装 MediaPipe FaceMesh，逐帧返回 478 点归一化关键点。
- `signalExtractors`：几何特征（嘴/眼/眉/俯仰角）→ 情绪（微笑/中性/专注/惊讶/皱眉）、点头检测（NodDetector）、紧张度（眨眼率/头部晃动/情绪切换综合加权）。
- `aggregator`：`BehaviorAggregator` 按 15s 分段聚合（情绪分布、点头、紧张度分段），`build()` 无副作用可重复调用。
- `useBehaviorAnalysis`：hook，驱动「开流 → 加载模型 → 逐帧提取 → 聚合 → 上报」，失败静默降级；提供 `liveStress` 实时紧张度与 `cameraStream`（供预览复用）。

---

## 核心业务流程

### 登录与鉴权（WPS OAuth 唯一登录）

登录唯一方式为 WPS 开放平台 OAuth，无账号密码注册/登录：

```
访问受保护路由 → ProtectedRoute 检查 token/user
  未登录 → 重定向 /login → LoginPage 调 GET /api/auth/wps/authorize 拿授权 URL → 整页跳转 WPS
  WPS 授权回调 → 后端处理 → 302 回前端（带一次性 oauth_code；失败带 error）
  前端 exchangeWPS(oauth_code) → 换取应用令牌 + 用户信息 → 写 localStorage → 更新 AuthContext → 跳首页
```

规则：

1. 所有业务接口统一带 `Authorization: Bearer <JWT>`。
2. 任一请求返回 401 → 前端清除令牌与用户缓存，并路由级跳转登录页（不整页刷新）。
3. 应用令牌过期以 401 为准，前端不做本地倒计时。
4. WPS 云文档类接口（`/api/wps/*`）子授权失效时不清除整体登录态（`skipAuthClear`），避免把用户整体登出。
5. 未配置 WPS 开放平台时授权接口返回 503，登录页展示错误。

### 创建面试（面试准备）

单页「面试间准备」，四卡片 + 底部开始按钮：

1. **面试岗位（JD）**：文本直填；图片 OCR 识别自动填充；文件上传（经后端 `/api/uploads` OSS 代理）。岗位名由前端取 JD 首个非空行（超 20 字截断）兜底展示。
2. **个人简历**：文本直填；文件上传后浏览器本地解析（mammoth/pdfjs）附文本；从简历库选择（`/api/resumes`）；从 WPS 云文档导入（`/api/wps/cloud-files` 拉取候选 → `/api/wps/cloud-files/import` 后端下载返回 base64，复用同一解析逻辑）。
3. **选择题库**：从题库勾选题目，硬上限 10 题、推荐 5~8；全部来自同一 `job_tag` 时提示 AI 会补充其他项目的题目。
4. **提交**：校验 JD 与简历非空 → 有选中题目走 `POST /api/interviews/from-bank`（题库练习，携带岗位/简历参与定制），否则走 `POST /api/interviews`；两者均固定 `mode=mixed`、`input_mode=voice` → `POST /api/interviews/:id/start` → 跳转房间页。

### 面试房间交互（核心）

一条 WebSocket 贯穿整场，**语音为唯一作答方式**（Voice-only product，历史 text 场次也以语音播报/作答）：

```
加载会话详情 GET /api/interviews/:id → 连接 WS(/ws/interviews/:id?token=)
  ├─ 收到 session_started → 启动已用时间计时；重连成功时排空断线暂存的答案
  ├─ 收到 question / follow_up → 追加面试官轮次 → TTS 朗读（超长分段；可静音 / 重播）
  ├─ 用户按住说话（pointer 按下）→ 录音 → 转 PCM → /api/speech/asr 转写 → WS 发送 answer（带 voice_duration_ms）
  ├─ 收到 status=thinking → 展示思考态
  ├─ 收到 closing → 播报收尾语后进入报告页（自然完成）
  ├─ 收到 done → 停止行为分析 → 进入报告页
  └─ 收到 error（not_found / invalid_state）→ 停止自动重连，展示明确提示

关键状态（页面内 ref，不持久化）：
  · speechVersionRef：语音操作版本号，过期 TTS / 录音回调一律丢弃，防止竞态
  · pendingAnswersRef：断线期间答案队列，重连成功后补发
  · pausedRef / accumulatedPausedMsRef：暂停账本，已用时间暂停冻结、恢复累计
  · failedAudioRef：识别失败暂存的音频，可「重试识别」或「放弃重录」
  · currentQuestionRef / lastInterviewerMsgRef：当前问题 / 末条面试官消息（去重展示与重播）
```

规则：

1. **时长**：预计 40 分钟（`ESTIMATED_MINUTES=40`），**非硬性上限**——时长随回答情况浮动、不强制结束；顶栏计时仅展示「已用时间」（暂停冻结、恢复累计）。
2. **录音中不允许暂停**；`thinking` / `paused` 等状态忽略录音与 TTS 播报。
3. **行为分析为产品必开能力**：房间页摄像头无条件开启，全程分析面试者行为（情绪分布、点头、紧张度），数据仅作报告辅助参考、不计入评分；摄像头预览复用同一路流。
4. **断线自动重连**：退避策略 1s/2s/4s/8s/8s；`session_started` 到达后排空 `pendingAnswers` 补发；退避耗尽降级为手动「重新连接」。致命错误（not_found / invalid_state）不重连。
5. **TTS 单次合成长度上限 300 字**（`TTS_MAX_RUNES`），超长问题按断句分段合成并顺序播放。
6. **ASR 失败处理**：识别为空或服务异常时暂存音频，可「重试识别」或「放弃重录」；502 提示「语音服务暂不可用」。
7. **结束**：收到 `done` / `closing` 停止行为分析并跳报告页；用户手动结束 → 确认后 `POST /api/interviews/:id/end` → 跳报告页；「跳过问题」发 `skip_question`，是否结束由后端决定。
8. **实时转写展示**：只展示最新一题及其紧随其后的回答（不滚动全量）。

### 报告与成长

- **ReportPage**：轮询 `GET /api/interviews/:id/report`（10s 一次、最多 30 次）直至报告可用；四维分数（表达能力/逻辑结构/内容质量/岗位匹配）+ 总分 + 优点/问题/建议；表达分析 `GET /api/interviews/:id/expression`（语速、口头禅、平均句长/字数）；行为信号 `GET /api/interviews/:id/behavior`（情绪分布、点头、紧张度走势，注明仅供参考不计分）；报告失败可重试 `POST /api/interviews/:id/report/retry`；支持发邮箱：`GET /api/wps/primary-email` 取收件人 → 确认 → `POST /api/interviews/:id/report/email`。
- **TrendsPage**：`GET /api/analytics/trends`，Recharts 柱状图展示各维度分与总分，可按岗位标签 / 面试模式（行为/技术/综合）/ 来源（常规/题库练习）筛选。
- **InterviewDetailPage**：展示面试信息与「主问题→追问→回答」追问树；draft 需 `start` 生成题目、ready 可直接进房间、in_progress 继续、completed 看报告；可将本场题目存入题库 `POST /api/questions/from-session/:id`。

---

## 数据模型（前端状态与本地存储）

### localStorage

||||
|---|---|---|
|**key**<br>|**内容**<br>|**说明**<br>|
|`auth_token`<br>|JWT 字符串<br>|请求鉴权；401 清除并路由级跳登录<br>|
|`auth_user`<br>|用户信息 JSON<br>|登录用户缓存，供 AuthContext 初始化<br>|

### 直播态（页面 ref，不持久化）

||||
|---|---|---|
|**状态**<br>|**内容**<br>|**说明**<br>|
|`speechVersionRef`<br>|语音操作版本号<br>|防止过期 TTS / 录音回调覆盖新状态<br>|
|`pendingAnswersRef`<br>|断线期间答案队列<br>|重连成功后排空补发<br>|
|`pausedRef` / `accumulatedPausedMsRef`<br>|暂停账本与累计暂停时长<br>|已用时间暂停冻结、恢复累计<br>|
|`currentQuestionRef` / `lastInterviewerMsgRef`<br>|当前问题 / 末条面试官消息<br>|去重展示与重播<br>|
|`failedAudioRef`<br>|识别失败的音频暂存<br>|支持「重试识别」或「放弃重录」<br>|

### 前端模型（类型定义见 `api/` 各模块）

`User` / `AuthResponse` / `Interview` / `InterviewListItem` / `InterviewTurn` / `InterviewQuestion` / `CreateInterviewInput` / `CreateFromBankInput` / `Question` / `ReportResult` / `InterviewFeedback` / `ExpressionResult` / `BehaviorPayload` / `BehaviorResult` / `TrendsData` / `ResumeFile` / `WpsCloudFile` / `UploadOut` / `PreCheckOut` / `Profile` 等，与后端接口字段一一对应。

---

## 前端路由定义

||||
|---|---|---|
|**路径**<br>|**页面**<br>|**说明**<br>|
|`/login`<br>|LoginPage<br>|WPS 登录（匿名）<br>|
|`/`<br>|WelcomePage<br>|落地页；已登录跳 `/interviews/new`<br>|
|`/history`<br>|InterviewListPage<br>|历史列表（需登录）<br>|
|`/questions`<br>|QuestionBankPage<br>|题库管理（需登录）<br>|
|`/interviews/new`<br>|CreateInterviewPage<br>|创建面试（需登录）<br>|
|`/interviews/:id`<br>|InterviewDetailPage<br>|面试详情（需登录）<br>|
|`/interviews/:id/room`<br>|InterviewRoomPage<br>|直播房间（需登录）<br>|
|`/interviews/:id/report`<br>|ReportPage<br>|评分报告（需登录）<br>|
|`/trends`<br>|TrendsPage<br>|成长趋势（需登录）<br>|
|`*`<br>|NotFoundPage<br>|404（匿名）<br>|

除 `/login`、`/`、`*` 外，其余页面均包在 `ProtectedRoute` 内。

---

## 接口约定（前端 API 层）

### 统一封装

- **后端地址解析**：显式设置 `VITE_API_BASE` 时优先，否则跟随当前页面 hostname（默认 `:18080`）；WebSocket 源由 API 基址推导（http→ws）。
- **`fetchJSON`**：统一 20s 超时；自动附加 `Authorization: Bearer`；FormData 不覆盖 Content-Type（保证 multipart 可解析）；401 自动清理登录态并派发路由级跳转事件（`skipAuthRedirect` / `skipAuthClear` 例外）；`ApiError` 承载状态码与原始文案，`toUserMessage` 做中文化映射。
- **语音 / OCR / 图片导入**走原生 `fetch` + multipart，避免 JSON 头污染。

### 依赖的后端接口（前端视角）

以下为 `frontend/src/api/*` 实际调用、且与后端路由一一对应的接口清单。

| 模块 | 方法与路径 | 用途 |
|------|-----------|------|
| auth | `GET /api/auth/wps/authorize` | 获取 WPS 授权 URL（整页跳转） |
| auth | `POST /api/auth/wps/exchange` | 用一次性 oauth_code 换取 token + user |
| auth | `GET /api/auth/me` | 拉取当前用户资料（refreshUser） |
| interviews | `POST /api/interviews` | 创建面试 |
| interviews | `GET /api/interviews` | 面试列表 |
| interviews | `POST /api/interviews/from-bank` | 题库练习创建 |
| interviews | `GET /api/interviews/:id` | 面试详情（含 questions / turns） |
| interviews | `POST /api/interviews/:id/start` | 开始（draft 生成题目后进入） |
| interviews | `POST /api/interviews/:id/end` | 用户确认结束 |
| interviews | `DELETE /api/interviews/:id` | 删除面试及其全部关联数据 |
| interviews | `GET /api/interviews/:id/report` | 获取评分报告（轮询） |
| interviews | `POST /api/interviews/:id/report/retry` | 报告失败重试 |
| interviews | `POST /api/interviews/:id/report/email` | 报告摘要发 WPS 邮箱 |
| expression | `GET /api/interviews/:id/expression` | 表达分析（语速/口头禅/句长） |
| behavior | `GET /api/interviews/:id/behavior` | 行为信号（情绪/点头/紧张度） |
| behavior | `POST /api/interviews/:id/behavior` | 上报行为聚合数据 |
| questions | `GET /api/questions` | 题库列表（starred/job_tag/q/dimension 过滤） |
| questions | `PATCH /api/questions/:id` | 编辑题干/答案/岗位标签/维度/收藏 |
| questions | `DELETE /api/questions/:id` | 删除单题 |
| questions | `POST /api/questions/batch-delete` | 批量删除 |
| questions | `POST /api/questions/from-session/:id` | 面试题目存入题库 |
| questions | `POST /api/questions/import/parse` | 导入解析（文本/图片 OCR） |
| questions | `POST /api/questions/import/confirm` | 导入确认落库 |
| questions | `POST /api/questions/question-bank/focused` | 按维度聚焦取题（已封装，当前页面未调用，预留） |
| speech | `POST /api/speech/asr` | 语音转写（PCM multipart） |
| speech | `POST /api/speech/tts` | 文本合成语音（返回音频 blob） |
| ocr | `POST /api/ocr/recognize` | 图片 OCR（multipart） |
| uploads | `POST /api/uploads` | 简历/JD 文件上传（OSS 代理，kind=resume/jd） |
| resumes | `GET /api/resumes` | 简历库列表 |
| resumes | `POST /api/resumes` | 上传简历（文件 + 前端解析文本） |
| resumes | `PATCH /api/resumes/:id` | 重命名 |
| resumes | `DELETE /api/resumes/:id` | 删除简历 |
| wps | `GET /api/wps/cloud-files` | WPS 云文档简历候选（可 keyword 搜索） |
| wps | `POST /api/wps/cloud-files/import` | 云文档下载为 base64（复用解析） |
| wps | `GET /api/wps/primary-email` | 获取主邮箱（发报告前确认收件人） |
| analytics | `GET /api/analytics/trends` | 成长趋势（job_tag/mode/source 过滤） |
| precheck | `POST /api/precheck` | 简历-JD 匹配预检（已封装，当前页面未调用，预留） |
| profile | `GET /api/profile` | 用户画像（弱项维度）（已封装，当前页面未调用，预留） |

### WebSocket 协议（前端视角）

- **连接**：`GET /ws/interviews/:id?token=<JWT>`（无 CORS 预检）。同一场一条连接；服务端心跳超时只断开连接，前端按退避重连。

- **前端 → 后端**：

||||
|---|---|---|
|**type**<br>|**说明**<br>|
|`answer`<br>|提交回答，携带 `content` 与可选 `voice_duration_ms`<br>|
|`skip_question`<br>|跳过当前题<br>|

- **后端 → 前端**：

||||
|---|---|---|
|**type**<br>|**说明**<br>|
|`session_started`<br>|直播开始（重连成功也发送，用于复位退避、启动计时、补发暂存答案）<br>|
|`question` / `follow_up`<br>|主问题 / 追问（携带 `progress` 题号时同步更新进度）<br>|
|`status`<br>|`thinking` 思考态或错误文案<br>|
|`closing`<br>|自然完成：播报收尾语（或静音）后跳报告页<br>|
|`done`<br>|面试结束，跳转报告页<br>|
|`error`<br>|携带 `code`：`not_found` / `invalid_state`（致命，不重连）或 `error`<br>|

- **断连与补发**：`onClose` 按退避 `[1000, 2000, 4000, 8000, 8000]` 重连；`session_started` 到达后排空 `pendingAnswersRef` 补发并提示「连接已恢复，暂存回答已发送」；退避耗尽降级为手动「重新连接」；致命错误不再自动重连。

---

## 部署与工程

- **构建**：Vite 生产构建（`tsc -b && vite build`）产出静态资源，部署到 Nginx 等静态服务器即可，无原生端、无 PWA。
- **开发**：固定端口 5174 开发服务器（`strictPort`，5173 常被占用），浏览器直接访问 `http://localhost:5174`。
- **后端地址**：构建/运行时经 `VITE_API_BASE` 显式指向后端；未设置时跟随当前页面 hostname（默认 `:18080`）。
- **单测**：Vitest 覆盖——API 错误映射与 401 处理、行为信号提取/聚合/`useBehaviorAnalysis`、追问树/来源/入口纯函数、弹窗等组件。
- **检查**：`npm run lint`（oxlint）+ `tsc` 类型检查，纳入工程命令。

---

*本文档依据前端代码现状整理（纯 Web 端，无手机端/原生壳）；接口与字段以代码及后端接口文档为准。*
