# V11 体验打磨（P1/P2 修复批次）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 7 项 P1/P2 体验问题：识别失败重试、语音→文字显式切换、移动端导航、语音 busy 提交提示、退出登录关 WS、结束面试进度提示、报告自动轮询。

**Architecture:** 全部前端改动。`InterviewRoomPage.tsx` 5 项（重试/切换/busy 提示/退登关 WS/结束文案）；`ReportPage.tsx` 轮询；7 个页面的顶栏导航链接加 `header-nav-link` 类 + CSS 移动端隐藏。零后端改动、无迁移。

**Tech Stack:** React/Vite TS、既有 design tokens、`interviewSocket`/`getReport` 等既有 API。

## Global Constraints

- Spec: `docs/superpowers/specs/2026-08-17-ux-polish-v11-design.md`
- 分支 `feat/v11-ux-polish` from main HEAD
- 识别失败重试：失败时保留 audio blob（`failedAudioRef`），「重试识别」重新调 ASR 不重说；新录音清 ref
- 语音→文字切换：`textModeOverride` 本地 state，不写库；`inputModeRef` 随 override 同步（effect）；override 后隐藏按住说话区
- 移动端：`@media (max-width: 599px)` 隐藏 `.header-nav-link`（导航链接 + header-cta）；退出按钮（保持 `interview-header-link` 无 nav 类）保留；Room 页加「返回列表」链接
- busy 提示：`handleSubmit` 语音 busy 早退处 `setStatusLine('正在识别语音，请稍候')`
- 退登关 WS：`user === null && !loadingInterview` → close socket + 清 retry timer + navigate('/login')
- 结束文案：`handleForceEnd` 开头 `setStatusLine('正在生成报告，请稍候…')`
- 报告轮询：10s 间隔、60s（6 次）超时；轮询中「报告正在生成中…（自动刷新中）」、超时「报告生成失败，可点击下方按钮重新生成」；重试成功停轮询、仍 false 重启轮询；卸载清定时器
- 零后端改动；`go test ./... -count=1 -p 1` 全绿（回归）+ `npm run build` 零 TS 错误

---

## File map

| Path | Responsibility |
|------|----------------|
| `frontend/src/pages/InterviewRoomPage.tsx` | 修复 1/2/4/5/6 |
| `frontend/src/pages/ReportPage.tsx` | 修复 7（轮询 + 文案） |
| `frontend/src/pages/InterviewPages.css` | `.header-nav-link` + 移动端 media query |
| `frontend/src/pages/{Create,InterviewDetail,InterviewList,QuestionBank,Report,Trends}InterviewPage*.tsx` | 导航链接加 `header-nav-link` 类（Room 在 Task 1 一并处理） |
| `docs/superpowers/specs/2026-08-17-ux-polish-v11-design.md` | Status → Implemented |

---

### Task 1: 房间页五项修复（重试/切换/busy 提示/退登/结束文案）

**Files:**
- Modify: `frontend/src/pages/InterviewRoomPage.tsx`

**Interfaces:**
- Consumes: 既有 `useAuth()`（含 `user`）、`transcribeAudio`、`connectInterviewWS`、`getToken`
- Produces: 无新导出

- [ ] **Step 1: 新增 refs/state**

在现有 refs 区（`recordStartRef` 附近）加：

```ts
const failedAudioRef = useRef<Blob | null>(null);
const retryingASR = useState(false)[0]; // 见 Step 2：需要 setter，写成两个声明
```

实际写法（state 分开）：

```ts
const [retryingASR, setRetryingASR] = useState(false);
const [textModeOverride, setTextModeOverride] = useState(false);
const failedAudioRef = useRef<Blob | null>(null);
```

- [ ] **Step 2: 识别失败重试**

`handleStopRecording` 改造——audio 提到 try 外捕获，失败保留：

```ts
async function handleStopRecording() {
  const recorder = voiceRecorderRef.current;
  if (!recorder) {
    voiceCancelRef.current = true;
    return;
  }
  voiceRecorderRef.current = null;
  setVoicePhase('transcribing');
  setStatusLine('正在识别语音...');
  let audio: Blob | undefined;
  try {
    const durationMs = recordStartRef.current
      ? Date.now() - recordStartRef.current
      : undefined;
    recordStartRef.current = null;
    audio = await recorder.stop();
    const { text } = await transcribeAudio(audio);
    const trimmed = text.trim();
    if (!trimmed) {
      failedAudioRef.current = audio;
      setVoicePhase('idle');
      setStatusLine('未识别到内容，可重试识别或重新录音');
      return;
    }
    setVoicePhase('sending');
    setStatusLine('');
    submitAnswer(trimmed, durationMs);
  } catch (err) {
    if (audio) {
      failedAudioRef.current = audio;
    }
    setVoicePhase('idle');
    setStatusLine(
      err instanceof ApiError && err.status === 502
        ? '语音服务暂不可用，请使用文字作答'
        : '识别失败，可重试识别或重新录音',
    );
  } finally {
    voiceActiveRef.current = false;
    voiceReadyRef.current = false;
  }
}
```

新增重试 handler：

```ts
async function handleRetryASR() {
  const audio = failedAudioRef.current;
  if (!audio || retryingASR) return;
  setRetryingASR(true);
  setStatusLine('正在重新识别…');
  try {
    const { text } = await transcribeAudio(audio);
    const trimmed = text.trim();
    failedAudioRef.current = null;
    if (!trimmed) {
      setVoicePhase('idle');
      setStatusLine('仍未识别到内容，请重新录音');
      return;
    }
    setVoicePhase('sending');
    setStatusLine('');
    submitAnswer(trimmed);
  } catch (err) {
    setVoicePhase('idle');
    setStatusLine(
      err instanceof ApiError && err.status === 502
        ? '语音服务暂不可用，请使用文字作答'
        : '重试识别失败，可重新录音',
    );
  } finally {
    setRetryingASR(false);
  }
}
```

`handleStartRecording` 开头清 `failedAudioRef.current = null;`（新录音开始）。

**UI**（voice 控件区，`voicePhase === 'idle' && failedAudioRef.current` 时显示）：

```tsx
{failedAudioRef.current && voicePhase === 'idle' && (
  <div className="voice-room-retry">
    <button type="button" className="interview-inline-link" onClick={() => void handleRetryASR()} disabled={retryingASR}>
      {retryingASR ? '重试中…' : '重试识别'}
    </button>
    <button type="button" className="interview-inline-link" onClick={() => { failedAudioRef.current = null; setStatusLine(''); }}>
      放弃重录
    </button>
  </div>
)}
```

- [ ] **Step 3: 语音→文字显式切换**

新增 effect 同步 ref（`inputModeRef` 既有）：

```ts
const effectiveInputMode: InputMode =
  inputMode === 'voice' && !textModeOverride ? 'voice' : 'text';

useEffect(() => {
  inputModeRef.current = effectiveInputMode;
}, [effectiveInputMode]);
```

语音控件区（按住说话按钮所在块）改为 `{effectiveInputMode === 'voice' && (...)}`；同区域加切换按钮：

```tsx
{inputMode === 'voice' && (
  <div className="voice-room-controls">
    {effectiveInputMode === 'voice' ? (
      <>
        {/* 现有按住说话按钮 + phase 提示 */}
        <button type="button" className="interview-inline-link" onClick={() => { setTextModeOverride(true); setStatusLine('已切换为文字作答，本场生效'); }}>
          切换为文字作答
        </button>
      </>
    ) : (
      <button type="button" className="interview-inline-link" onClick={() => { setTextModeOverride(false); setStatusLine('已切回语音作答'); }}>
        切回语音作答
      </button>
    )}
  </div>
)}
```

textarea 标签与占位符按 `effectiveInputMode` 判定（替换现有 `inputMode === 'voice'` 判定）；发送按钮 disabled 里的 voice-busy 判定保持（用 `inputModeRef.current === 'voice'`，已被 effect 同步）。

- [ ] **Step 4: 语音 busy 提交提示**

`handleSubmit` 的 busy 早退（现有 `voicePhase === 'transcribing' || 'sending'` 分支）加提示：

```ts
if (
  inputModeRef.current === 'voice' &&
  (voicePhase === 'transcribing' || voicePhase === 'sending')
) {
  setStatusLine('正在识别语音，请稍候');
  return;
}
```

（其余早退条件不变；注意现有 return 分支结构，按文件实际顺序插入。）

- [ ] **Step 5: 退出登录关闭 WS**

`useAuth()` 解构加 `user`：

```ts
const { logout, user } = useAuth();
```

新增 effect：

```ts
useEffect(() => {
  if (user === null && !loadingInterview) {
    socketRef.current?.close();
    socketRef.current = null;
    if (retryTimerRef.current != null) {
      window.clearTimeout(retryTimerRef.current);
      retryTimerRef.current = null;
    }
    navigate('/login');
  }
}, [user, loadingInterview, navigate]);
```

- [ ] **Step 6: 结束面试进度提示**

`handleForceEnd` 开头（`setEnding(true)` 后）加：

```ts
setStatusLine('正在生成报告，请稍候…');
```

- [ ] **Step 7: 构建**

Run: `cd frontend && npm run build`
Expected: PASS（零 TS 错误）。

- [ ] **Step 8: 提交**

```bash
git add frontend/src/pages/InterviewRoomPage.tsx
git commit -m "feat(v11): room voice retry, text-mode switch, busy hint, logout ws close, end progress copy"
```

---

### Task 2: 报告自动轮询（ReportPage）

**Files:**
- Modify: `frontend/src/pages/ReportPage.tsx`

**Interfaces:**
- Consumes: `getReport(id)`（返回 `{available:false}` 或 feedback）、既有 `available/feedback/error` state
- Produces: 无新导出

- [ ] **Step 1: 轮询 refs 与函数**

新增：

```ts
const pollTimerRef = useRef<number | null>(null);
const pollCountRef = useRef(0);
const [pollFailed, setPollFailed] = useState(false);
```

```ts
const stopPolling = useCallback(() => {
  if (pollTimerRef.current != null) {
    window.clearInterval(pollTimerRef.current);
    pollTimerRef.current = null;
  }
}, []);

const startPolling = useCallback(() => {
  stopPolling();
  pollCountRef.current = 0;
  setPollFailed(false);
  pollTimerRef.current = window.setInterval(async () => {
    pollCountRef.current += 1;
    try {
      const result = await getReport(interviewId);
      if (result.available) {
        setFeedback(result.feedback);
        setAvailable(true);
        stopPolling();
      } else if (pollCountRef.current >= 6) {
        stopPolling();
        setPollFailed(true);
      }
    } catch {
      stopPolling();
      setError('报告加载失败，请稍后重试');
    }
  }, 10000);
}, [getReport, interviewId, setFeedback, setAvailable, stopPolling]);
```

（`setFeedback/setAvailable` 是 useState setter，稳定引用，可不进依赖；`getReport` 是模块函数稳定。）

- [ ] **Step 2: 初始加载触发轮询**

现有初始 effect（`getReport` 后 `available === false` 分支）加 `startPolling()`；effect 清理加 `stopPolling()`：

```ts
if (result.available) {
  ...
} else {
  setAvailable(false);
  startPolling();
}
...
return () => {
  cancelled = true;
  stopPolling();
};
```

- [ ] **Step 3: handleRetry 联动**

`handleRetry` 中：`available` → `stopPolling()`；仍 false → `startPolling()`：

```ts
if (result.available) {
  setFeedback(result.feedback);
  setAvailable(true);
  stopPolling();
} else {
  setFeedback(null);
  setAvailable(false);
  setPollFailed(false);
  setError('');
  startPolling(); // 重启轮询
}
```

- [ ] **Step 4: 文案区分**

`available === false` 分支文案改：

```tsx
<p>
  {pollFailed
    ? '报告生成失败，可点击下方按钮重新生成。'
    : '报告正在生成中，请稍候…（自动刷新中）'}
</p>
```

- [ ] **Step 5: 构建**

Run: `cd frontend && npm run build`
Expected: PASS。

- [ ] **Step 6: 提交**

```bash
git add frontend/src/pages/ReportPage.tsx
git commit -m "feat(v11): auto-poll report availability with timeout"
```

---

### Task 3: 移动端导航（7 页加类 + CSS）

**Files:**
- Modify: `frontend/src/pages/InterviewPages.css`
- Modify: `frontend/src/pages/CreateInterviewPage.tsx`, `InterviewDetailPage.tsx`, `InterviewListPage.tsx`, `QuestionBankPage.tsx`, `ReportPage.tsx`, `TrendsPage.tsx`（Room 已在 Task 1 处理）

**Interfaces:**
- Consumes: 既有 header 结构
- Produces: `.header-nav-link` 类（导航链接标记，移动端隐藏）

- [ ] **Step 1: CSS 加类与 media query**

`InterviewPages.css` 末尾加：

```css
/* 顶栏导航链接（桌面显示，移动端隐藏；底部标签栏承担导航） */
@media (max-width: 599px) {
  .header-nav-link {
    display: none;
  }
}
```

- [ ] **Step 2: 各页导航链接加类**

6 个页面（Create/Detail/List/Bank/Report/Trends）中所有**导航用** `Link`（`to="..."` 的）className 改为 `interview-header-link header-nav-link`；**退出按钮**（`onClick={logout}`）保持 `interview-header-link` 不变。List 页的 `interview-header-cta`（新建面试链接）加 `header-nav-link`（移动端隐藏，底部标签栏有「新建」）。

涉及文件与位置（每页的导航 Link 数量不同，逐个处理）：
- `CreateInterviewPage.tsx`：3 个导航 Link（题库/成长分析/返回列表）
- `InterviewDetailPage.tsx`：3 个（题库/成长分析/返回列表）
- `InterviewListPage.tsx`：2 个 Link + 1 个 CTA（题库/成长分析/新建）
- `QuestionBankPage.tsx`：3 个（返回列表/题库 aria-current/成长分析）
- `ReportPage.tsx`：2 个（题库/成长分析）+ 详情链接（to={`/interviews/${id}`} 或返回列表，按其实际结构）
- `TrendsPage.tsx`：3 个（返回列表/题库/成长分析）
- `InterviewRoomPage.tsx`：Task 1 已加「返回列表」——此任务确认其导航 Link 也带 `header-nav-link`

- [ ] **Step 3: Room 页「返回列表」**

（若 Task 1 未加）`InterviewRoomPage.tsx` 顶栏在「详情」链接前加：

```tsx
<Link className="interview-header-link header-nav-link" to="/">
  返回列表
</Link>
```

- [ ] **Step 4: 构建**

Run: `cd frontend && npm run build`
Expected: PASS。

- [ ] **Step 5: 提交**

```bash
git add frontend/src/pages/InterviewPages.css frontend/src/pages/
git commit -m "feat(v11): hide header nav links on mobile, add room back link"
```

---

### Task 4: 验收

**Files:**
- Modify: `docs/superpowers/specs/2026-08-17-ux-polish-v11-design.md`

- [ ] **Step 1: 全量后端测试（回归）**

Run: `cd backend && go test ./... -count=1 -p 1`
Expected: 全部 PASS（前端改动不影响后端，回归确认）。

- [ ] **Step 2: 前端构建**

Run: `cd frontend && npm run build`
Expected: PASS。

- [ ] **Step 3: 手工冒烟（可选）**

语音识别失败 → 重试识别/放弃重录；语音模式切换文字；移动端视口顶栏仅品牌+退出；语音 busy 提交提示；退出登录跳转；结束面试显示进度文案；报告未就绪自动轮询至展示。

- [ ] **Step 4: 更新 spec 状态**

`docs/superpowers/specs/2026-08-17-ux-polish-v11-design.md` 的 `**Status:** Draft for user review` 改为 `**Status:** Implemented on feat/v11-ux-polish`。

- [ ] **Step 5: 提交并合并**

```bash
git add docs/superpowers/specs/2026-08-17-ux-polish-v11-design.md
git commit -m "docs(v11): mark spec implemented"
# 回到主仓库合并
git merge --no-ff feat/v11-ux-polish -m "merge: V11 UX polish"
```

---

## Spec coverage self-review

| Spec section | Task |
|--------------|------|
| §4 识别失败重试 | T1 Step 2 |
| §5 语音→文字切换 | T1 Step 3 |
| §6 移动端导航 | T3 |
| §7 busy 提交提示 | T1 Step 4 |
| §8 退登关 WS | T1 Step 5 |
| §9 结束进度提示 | T1 Step 6 |
| §10 报告轮询 | T2 |
| §11 U1–U8 | T1–T4 |

## Placeholder scan

所有步骤含具体代码/命令，无 TBD。执行时需确认的点：
- `handleStopRecording` 现有代码结构与 Step 2 的替换（audio 变量作用域提升、finally 复位 voice refs）
- `handleSubmit` 早退分支的精确插入位置（按文件实际结构）
- ReportPage 现有 effect 的 `cancelled` 清理与 `stopPolling` 并存
- `inputModeRef` effect 与 `loadAndConnect` 设置 inputModeRef 的先后（override 只在用户点击后生效）
