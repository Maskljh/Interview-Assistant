# 新建面试 JD 支持图片 OCR Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新建面试页职位描述（JD）新增图片上传，用阿里云 OCR 识别文字并自动填入 JD 文本框（可继续编辑）。

**Architecture:** 后端新增纯 OCR 识别接口 `POST /api/ocr/recognize`（复用现有 `internal/ocr.Client`，不重复构造），multipart 图片 → 校验 → `Recognize` → `{"text":"..."}`。前端新建面试页 JD 区新增图片入口，调 `recognizeImage` → 自动 `setJobJd(text)`。简历区不变，图片不存 OSS。

**Tech Stack:** Go 1.24 + Gin + MySQL（后端）；React 19 + TypeScript + Vite + vitest/jsdom（前端）；阿里云 OCR 官方 SDK（已有）。

## Global Constraints

- 仅 JD 支持图片 OCR；简历（resume）区域**不改**。
- OCR 识别文本**自动填入** JD 文本框；填入后可继续编辑；`setPrecheckStale(true)`。
- 图片**不上传 OSS**（识别文本即用即弃）。
- 图片大小 ≤ 5MB，类型仅 `image/jpeg` / `image/png` / `image/webp`。
- OCR 不可用时提示精确文案：`图片识别失败，请改用文本粘贴`；文本粘贴/文档上传不受影响。
- 复用现有 `internal/ocr.Client` 接口与 `main.go` 的 `ocrClient`，不重复构造。
- 用户可见文案精确：`或上传 JD 图片`、`图片识别失败，请改用文本粘贴`、`未识别到文字，请尝试更清晰的图片`。
- 提交信息遵循仓库习惯（`feat(ocr): ...` / `feat(interview): ...`）。
- 每个任务须通过对应测试：后端 `go test ./...`、前端 `npm test`；前端任务另须 `npx tsc --noEmit -p tsconfig.app.json`。

---

### Task 1: 后端 OCR 识别接口（`internal/ocr/handler.go` + 测试）

**Files:**
- Create: `backend/internal/ocr/handler.go`
- Create: `backend/internal/ocr/handler_test.go`

**Interfaces:**
- Consumes: `ocr.Client`（已有，`Recognize(ctx, image []byte) (string, error)`）、`auth.Middleware(secret)`
- Produces:
  - `func RegisterRoutes(r *gin.Engine, secret string, client Client)` — 注册 `POST /api/ocr/recognize`
  - 响应：`200 {"text":"..."}`；`400 {"error":"image is too large"}` / `400 {"error":"unsupported image type"}` / `400 {"error":"file is required"}`；`502 {"error":"image recognition unavailable, please use text input"}`

- [ ] **Step 1: 写失败测试**

`backend/internal/ocr/handler_test.go`：

```go
package ocr_test

import (
	"bytes"
	"encoding/json"
	"fmt"
	"mime/multipart"
	"net/http"
	"net/http/httptest"
	"testing"

	"github.com/gin-gonic/gin"
	"github.com/interview-assistant/backend/internal/auth"
	"github.com/interview-assistant/backend/internal/ocr"
)

// fakeClient returns a fixed text; implements ocr.Client.
type fakeClient struct{}

func (f *fakeClient) Recognize(ctx context.Context, image []byte) (string, error) {
	return "识别出的 JD 文本", nil
}

type nilClient struct{}

func (n *nilClient) Recognize(ctx context.Context, image []byte) (string, error) {
	return "", fmt.Errorf("no client")
}

func testRouter(t *testing.T, client ocr.Client) *gin.Engine {
	t.Helper()
	gin.SetMode(gin.TestMode)
	r := gin.New()
	secret := "test-secret"
	// 需要注册一个用户来拿 token；复用简单方式：直接构造 token
	r.GET("/healthz", func(c *gin.Context) { c.JSON(200, gin.H{"ok": true}) })
	ocr.RegisterRoutes(r, secret, client)
	return r
}

func newMultipartImage(t *testing.T, content []byte) (*bytes.Buffer, string) {
	t.Helper()
	var body bytes.Buffer
	w := multipart.NewWriter(&body)
	part, err := w.CreateFormFile("file", "jd.png")
	if err != nil {
		t.Fatalf("create form file: %v", err)
	}
	_, _ = part.Write(content)
	if err := w.Close(); err != nil {
		t.Fatalf("close multipart: %v", err)
	}
	return &body, w.Boundary()
}

// 生成有效 token（含 secret 匹配）
func authToken(t *testing.T, secret string) string {
	t.Helper()
	// 用项目已有 auth 包生成
	return ""
}
```

> 注：token 生成需参考 `backend/internal/auth/jwt.go` 的签名方式。若不便在测试内生成，可改用「先注册用户拿 token」的方式（参考 `internal/question/service_test.go` 的 `registerUser`）。**实现者自选其一**，目标是测试能带有效 `Authorization: Bearer <token>` 打到受保护路由。

- [ ] **Step 2: 运行测试确认失败**

Run: `cd backend && go test ./internal/ocr/ -run 'TestRecognize|Test' -v`
Expected: FAIL（`RegisterRoutes` 未定义 / 测试未写完整）

- [ ] **Step 3: 写实现 `handler.go`**

`backend/internal/ocr/handler.go`：

```go
package ocr

import (
	"errors"
	"io"
	"net/http"
	"strings"

	"github.com/gin-gonic/gin"
	"github.com/interview-assistant/backend/internal/auth"
)

const maxImageBytes = 5 << 20

func RegisterRoutes(r *gin.Engine, secret string, client Client) {
	h := &handler{client: client}
	protected := r.Group("/api/ocr")
	protected.Use(auth.Middleware(secret))
	protected.POST("/recognize", h.Recognize)
}

type handler struct {
	client Client
}

func (h *handler) Recognize(c *gin.Context) {
	if h.client == nil {
		c.JSON(http.StatusBadGateway, gin.H{"error": "image recognition unavailable, please use text input"})
		return
	}
	file, err := c.FormFile("file")
	if err != nil {
		c.JSON(http.StatusBadRequest, gin.H{"error": "file is required"})
		return
	}
	if file.Size > maxImageBytes {
		c.JSON(http.StatusBadRequest, gin.H{"error": "image is too large"})
		return
	}
	f, err := file.Open()
	if err != nil {
		c.JSON(http.StatusBadRequest, gin.H{"error": "could not read image"})
		return
	}
	defer f.Close()
	image, err := io.ReadAll(f)
	if err != nil {
		c.JSON(http.StatusBadRequest, gin.H{"error": "could not read image"})
		return
	}
	ct := http.DetectContentType(image)
	if ct != "image/jpeg" && ct != "image/png" && ct != "image/webp" {
		c.JSON(http.StatusBadRequest, gin.H{"error": "unsupported image type"})
		return
	}
	text, err := h.client.Recognize(c.Request.Context(), image)
	if err != nil {
		c.JSON(http.StatusBadGateway, gin.H{"error": "image recognition unavailable, please use text input"})
		return
	}
	c.JSON(http.StatusOK, gin.H{"text": text})
}
```

（`errors`/`strings` 若未使用则不导入——按 `go build` 报错清理。）

- [ ] **Step 4: 完善测试并跑通**

补全 `handler_test.go` 的 token 生成（参考 `internal/question/service_test.go` 的 `registerUser` 模式：在 `testRouter` 里也注册 `user.RegisterRoutes`），并写 4 个用例：

```go
func TestRecognizeImage(t *testing.T) {
	r := testRouter(t, &fakeClient{})
	token := registerUser(t, r)
	body, boundary := newMultipartImage(t, []byte("fake-png-bytes"))
	w := httptest.NewRecorder()
	req := httptest.NewRequest(http.MethodPost, "/api/ocr/recognize", body)
	req.Header.Set("Content-Type", "multipart/form-data; boundary="+boundary)
	req.Header.Set("Authorization", "Bearer "+token)
	r.ServeHTTP(w, req)
	if w.Code != http.StatusOK {
		t.Fatalf("status = %d, body = %s", w.Code, w.Body.String())
	}
	var resp struct {
		Text string `json:"text"`
	}
	if err := json.Unmarshal(w.Body.Bytes(), &resp); err != nil {
		t.Fatalf("decode: %v", err)
	}
	if resp.Text != "识别出的 JD 文本" {
		t.Fatalf("text = %q", resp.Text)
	}
}

func TestRecognizeNilClientReturns502(t *testing.T) {
	r := testRouter(t, nil)
	token := registerUser(t, r)
	body, boundary := newMultipartImage(t, []byte("x"))
	w := httptest.NewRecorder()
	req := httptest.NewRequest(http.MethodPost, "/api/ocr/recognize", body)
	req.Header.Set("Content-Type", "multipart/form-data; boundary="+boundary)
	req.Header.Set("Authorization", "Bearer "+token)
	r.ServeHTTP(w, req)
	if w.Code != http.StatusBadGateway {
		t.Fatalf("status = %d, want 502", w.Code)
	}
}

func TestRecognizeOversizedReturns400(t *testing.T) {
	r := testRouter(t, &fakeClient{})
	token := registerUser(t, r)
	big := make([]byte, 5<<20+1) // > 5MB，与 handler 的 maxImageBytes 一致
	body, boundary := newMultipartImage(t, big)
	w := httptest.NewRecorder()
	req := httptest.NewRequest(http.MethodPost, "/api/ocr/recognize", body)
	req.Header.Set("Content-Type", "multipart/form-data; boundary="+boundary)
	req.Header.Set("Authorization", "Bearer "+token)
	r.ServeHTTP(w, req)
	if w.Code != http.StatusBadRequest {
		t.Fatalf("status = %d, want 400", w.Code)
	}
}

func TestRecognizeNonImageReturns400(t *testing.T) {
	r := testRouter(t, &fakeClient{})
	token := registerUser(t, r)
	body, boundary := newMultipartImage(t, []byte("plain text, not an image"))
	w := httptest.NewRecorder()
	req := httptest.NewRequest(http.MethodPost, "/api/ocr/recognize", body)
	req.Header.Set("Content-Type", "multipart/form-data; boundary="+boundary)
	req.Header.Set("Authorization", "Bearer "+token)
	r.ServeHTTP(w, req)
	if w.Code != http.StatusBadRequest {
		t.Fatalf("status = %d, want 400", w.Code)
	}
}
```

> `registerUser` 需在 `testRouter` 里同时注册 `user.RegisterRoutes(r, sqlDB, secret)` 以便走真实注册流程拿 token（参考 `internal/question/service_test.go` 的 `testRouter` + `registerUser`）。若 `user.RegisterRoutes` 需要 `*sql.DB`，`testRouter` 需接收 DB 或直接构造一个 token（用 `auth` 包的签名函数）。**实现者按仓库既有测试模式选最简方式。**

- [ ] **Step 5: 全量后端测试确认无回归**

Run: `cd backend && go test ./...`
Expected: 全部 PASS（含新增 ocr handler 测试）

- [ ] **Step 6: 提交**

```bash
git add backend/internal/ocr/handler.go backend/internal/ocr/handler_test.go
git commit -m "feat(ocr): add /api/ocr/recognize endpoint for image text recognition"
```

---

### Task 2: main.go 接线 OCR 路由

**Files:**
- Modify: `backend/cmd/server/main.go`

**Interfaces:**
- Consumes: `ocr.RegisterRoutes`（Task 1）、`ocrClient`（main.go 已有）
- Produces: `/api/ocr/recognize` 路由注册到现有 gin 引擎

- [ ] **Step 1: 注册路由**

`backend/cmd/server/main.go` 在 `question.RegisterRoutes(r, sqlDB, cfg.JWTSecret, llmClient, ocrClient)`（:103）之后加：

```go
	ocr.RegisterRoutes(r, cfg.JWTSecret, ocrClient)
```

- [ ] **Step 2: 构建验证**

Run: `cd backend && go build ./...`
Expected: 构建成功（`ocr` 包已 import，无需新增 import——检查是否已 `import "github.com/interview-assistant/backend/internal/ocr"`，若已用于构造 `ocrClient` 则已导入）

- [ ] **Step 3: 全量后端测试**

Run: `cd backend && go test ./...`
Expected: 全部 PASS

- [ ] **Step 4: 提交**

```bash
git add backend/cmd/server/main.go
git commit -m "feat(ocr): wire /api/ocr/recognize route in main"
```

---

### Task 3: 前端 API 客户端 `recognizeImage`

**Files:**
- Create: `frontend/src/api/ocr.ts`
- Test: `frontend/src/api/ocr.test.ts`

**Interfaces:**
- Consumes: `getApiBase`/`getToken`/`toUserMessage`/`ApiError`（`frontend/src/api/client.ts`）
- Produces:
  - `export async function recognizeImage(file: File): Promise<{ text: string }>` — multipart 原生 fetch 到 `/api/ocr/recognize`，返回 `{ text }`

- [ ] **Step 1: 写失败测试**

`frontend/src/api/ocr.test.ts`：

```ts
import { afterEach, describe, expect, it, vi } from 'vitest';
import { recognizeImage } from './ocr';
import { ApiError } from './client';

vi.mock('@capacitor/core', () => ({ Capacitor: { isNativePlatform: () => false } }));

// 让 getApiBase 稳定
vi.mock('./client', async (importOriginal) => {
  const actual = (await importOriginal()) as object;
  return {
    ...actual,
    getApiBase: () => 'http://test.local',
    getToken: () => 'test-token',
    toUserMessage: (status: number, raw: string) => raw,
  };
});

afterEach(() => {
  vi.restoreAllMocks();
});

describe('recognizeImage', () => {
  it('POSTs multipart and returns text on 200', async () => {
    const fetchMock = vi.fn(async (_url: string, init?: RequestInit) => {
      const body = init?.body as FormData;
      expect(body.get('file')).toBeInstanceOf(File);
      return {
        ok: true,
        status: 200,
        json: async () => ({ text: '识别出的 JD 文本' }),
      } as Response;
    });
    vi.stubGlobal('fetch', fetchMock);

    const file = new File(['x'], 'jd.png', { type: 'image/png' });
    const res = await recognizeImage(file);
    expect(res.text).toBe('识别出的 JD 文本');
    const url = fetchMock.mock.calls[0][0];
    expect(String(url)).toContain('/api/ocr/recognize');
  });

  it('throws ApiError with raw message on failure', async () => {
    vi.stubGlobal('fetch', vi.fn(async () => ({
      ok: false,
      status: 502,
      statusText: 'Bad Gateway',
      json: async () => ({ error: 'image recognition unavailable, please use text input' }),
    } as Response)));

    const file = new File(['x'], 'jd.png', { type: 'image/png' });
    await expect(recognizeImage(file)).rejects.toThrow(ApiError);
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd frontend && npx vitest run src/api/ocr.test.ts`
Expected: FAIL（`./ocr` 模块不存在）

- [ ] **Step 3: 写实现 `ocr.ts`**

`frontend/src/api/ocr.ts`：

```ts
import { ApiError, getApiBase, getToken, toUserMessage } from './client';

export async function recognizeImage(file: File): Promise<{ text: string }> {
  // multipart：原生 fetch，避免 fetchJSON 默认 JSON Content-Type
  const token = getToken();
  const form = new FormData();
  form.append('file', file);
  const res = await fetch(`${getApiBase()}/api/ocr/recognize`, {
    method: 'POST',
    headers: token ? { Authorization: `Bearer ${token}` } : undefined,
    body: form,
  });
  if (!res.ok) {
    let message = res.statusText || 'Request failed';
    try {
      const data = (await res.json()) as unknown;
      if (data && typeof data === 'object' && 'error' in data) {
        message = String((data as { error: unknown }).error);
      }
    } catch {
      // keep status text
    }
    throw new ApiError(res.status, toUserMessage(res.status, message), message);
  }
  return (await res.json()) as { text: string };
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd frontend && npx vitest run src/api/ocr.test.ts`
Expected: PASS（2 测试）

- [ ] **Step 5: 类型检查 + 提交**

Run: `cd frontend && npx tsc --noEmit -p tsconfig.app.json`
Expected: 无错误

```bash
git add frontend/src/api/ocr.ts frontend/src/api/ocr.test.ts
git commit -m "feat(interview): add recognizeImage API client for JD image OCR"
```

---

### Task 4: 新建面试页 JD 图片上传接入

**Files:**
- Modify: `frontend/src/pages/CreateInterviewPage.tsx`
- Test: `frontend/src/pages/CreateInterviewPage.test.tsx`（新建，若无则建）

**Interfaces:**
- Consumes: `recognizeImage`（Task 3）
- Produces: JD 区「或上传 JD 图片」入口，选图 → OCR → 填入 `jobJd` → `setPrecheckStale(true)`

- [ ] **Step 1: 写失败测试**

`frontend/src/pages/CreateInterviewPage.test.tsx`（若文件不存在则新建；参考 `AppNav.test.tsx` 的渲染方式，用 `MemoryRouter` + `AuthProvider`）：

```tsx
import { afterEach, describe, expect, it, vi } from 'vitest';
import { render, screen, fireEvent, waitFor } from '@testing-library/react';
import { MemoryRouter } from 'react-router-dom';
import CreateInterviewPage from './CreateInterviewPage';
import { AuthProvider } from '../auth/AuthContext';
import { recognizeImage } from '../api/ocr';

vi.mock('@capacitor/core', () => ({ Capacitor: { isNativePlatform: () => false } }));

vi.mock('../api/ocr', () => ({
  recognizeImage: vi.fn(async () => ({ text: '识别出的 JD 文本' })),
}));

vi.mock('../api/profile', () => ({
  fetchProfile: vi.fn(async () => ({ weak_dimensions: [], based_on_sessions: 0 })),
}));

function renderPage() {
  return render(
    <MemoryRouter>
      <AuthProvider>
        <CreateInterviewPage />
      </AuthProvider>
    </MemoryRouter>,
  );
}

afterEach(() => {
  vi.clearAllMocks();
});

describe('CreateInterviewPage JD image OCR', () => {
  it('shows JD image upload option', () => {
    renderPage();
    expect(screen.getByText('或上传 JD 图片')).toBeTruthy();
  });

  it('fills JD textarea with OCR text after selecting image', async () => {
    renderPage();
    const fileInput = screen.getByLabelText('或上传 JD 图片') as HTMLInputElement;
    const file = new File(['x'], 'jd.png', { type: 'image/png' });
    fireEvent.change(fileInput, { target: { files: [file] } });
    await waitFor(() => {
      expect((screen.getByLabelText('职位描述') as HTMLTextAreaElement).value).toBe('识别出的 JD 文本');
    });
    expect(recognizeImage).toHaveBeenCalledWith(file);
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd frontend && npx vitest run src/pages/CreateInterviewPage.test.tsx`
Expected: FAIL（「或上传 JD 图片」不存在 / `recognizeImage` 未接入）

- [ ] **Step 3: 接入 CreateInterviewPage.tsx**

1. import 增加：

```tsx
import { recognizeImage } from '../api/ocr';
```

2. 组件内新增 state（放在 `jdUploading` 附近）：

```tsx
  const [jdOcrName, setJdOcrName] = useState('');
  const [jdOcrRecognizing, setJdOcrRecognizing] = useState(false);
```

3. 新增处理函数（放在 `handleJdFile` 之后）：

```tsx
  async function handleJdImage(e: ChangeEvent<HTMLInputElement>) {
    const file = e.target.files?.[0];
    if (!file) return;
    setError('');
    setJdOcrRecognizing(true);
    setJdOcrName(file.name);
    try {
      const { text } = await recognizeImage(file);
      if (!text.trim()) {
        setError('未识别到文字，请尝试更清晰的图片');
        return;
      }
      setJobJd(text);
      setPrecheckStale(true);
    } catch (err) {
      const msg = err instanceof ApiError ? err.rawMessage : '';
      const ux = err instanceof ApiError ? err.message : '图片识别失败';
      if (msg.includes('unavailable') || ux.includes('改用文本粘贴')) {
        setError('图片识别失败，请改用文本粘贴');
      } else {
        setError(ux);
      }
    } finally {
      setJdOcrRecognizing(false);
      e.target.value = '';
    }
  }
```

4. JD 字段区（`handleJdFile` 的 `interview-file-row` 之后、`interview-field-hint` 之前）新增图片入口：

```tsx
            <div className="interview-file-row">
              <label className="interview-file-label" htmlFor="jd-image">
                或上传 JD 图片
              </label>
              <input
                id="jd-image"
                type="file"
                accept="image/jpeg,image/png,image/webp"
                onChange={handleJdImage}
                disabled={jdOcrRecognizing || loading}
              />
              {jdOcrRecognizing && (
                <span className="interview-file-progress">OCR 识别中…</span>
              )}
              {jdOcrName && !jdOcrRecognizing && (
                <span className="interview-file-name">
                  已识别：{jdOcrName}
                  {jobJd ? `（约 ${jobJd.length} 字）` : ''}
                </span>
              )}
            </div>
```

> 若 `interview-file-label` 样式不存在，可复用现有 `.interview-file-meta`/`.interview-file-name` 或加最小 CSS。`htmlFor="jd-image"` 与 `id="jd-image"` 关联，使 `getByLabelText('或上传 JD 图片')` 可定位。

- [ ] **Step 4: 类型检查 + 测试**

Run: `cd frontend && npx tsc --noEmit -p tsconfig.app.json && npm test`
Expected: 无类型错误；全部 PASS（含新增 CreateInterviewPage 测试）

- [ ] **Step 5: 提交**

```bash
git add frontend/src/pages/CreateInterviewPage.tsx frontend/src/pages/CreateInterviewPage.test.tsx
git commit -m "feat(interview): allow JD image OCR to fill job description on create page"
```

---

### Task 5: 全量回归验证 + 验收

**Files:**
- 无代码改动

- [ ] **Step 1: 后端全量**

Run: `cd backend && go test ./... && go build ./...`
Expected: 全部 PASS、构建成功

- [ ] **Step 2: 前端全量**

Run: `cd frontend && npm test && npm run build`
Expected: 全部 PASS、构建成功

- [ ] **Step 3: 对照 spec 逐条验收**

- [ ] 新建面试页 JD 区有「或上传 JD 图片」入口（Task 4）
- [ ] 选图 → OCR 识别 → 文本自动填入职位描述文本框（Task 3/4）
- [ ] 填入后可继续编辑，precheck 标记 stale（Task 4）
- [ ] OCR 不可用时提示「图片识别失败，请改用文本粘贴」，文本粘贴/文档上传不受影响（Task 4）
- [ ] 简历区无图片上传（Task 4 未动简历区）
- [ ] 图片不上传 OSS（无 signUpload/uploadToOSS 调用）
- [ ] 后端 `POST /api/ocr/recognize` 独立可用，题库导入 `/api/questions/import/parse` 不变（Task 1/2）

- [ ] **Step 4: 手工冒烟（若后端 + 前端服务可运行）**

新建面试页 `http://localhost:5174/interviews/new`：选一张 JD 截图 → OCR 识别 → 文本填入职位描述 → 可编辑 → 开始面试。OCR 链路依赖 DeepSeek 余额与否无关（纯 OCR，不调 LLM）。

- [ ] **Step 5: 汇报**

写最终报告至 `.superpowers/sdd/2026-08-24-jd-ocr/task-5-report.md`，列出验收逐条结果与验证证据。
